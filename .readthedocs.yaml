version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - pip install uv==0.10.8
    - uv sync --group docs
    - uv run --group docs python scripts/build_docs.py
    - mkdir -p "$READTHEDOCS_OUTPUT/html"
    - cp -a .build/docs/. "$READTHEDOCS_OUTPUT/html/"
